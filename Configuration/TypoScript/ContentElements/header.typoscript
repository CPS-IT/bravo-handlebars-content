tt_content.header >
tt_content.header = HANDLEBARSTEMPLATE
tt_content.header {
  # note: we dont have a dedicated template for header
  templateName = @ce-text
  dataProcessing {
    fields = header,header_layout,header_link,@headlines,hidden,space_before_class,uid
    10 = ceText
  }
}
